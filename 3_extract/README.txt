this folder contains code required to build a Docker container, which is then used to extract entities and relationships from PubMed abstracts on GPU enabled machines.

see https://hub.docker.com/r/rmillikin/kg_extract/tags for prebuilt images